---
import BaseLayout from "../../layouts/BaseLayout.astro";
import content from "../../content/pt.json";
const { terms, leadership, footer, nav, meta } = content;
---
<BaseLayout
  lang="pt" htmlLang={content.htmlLang}
  title={meta.terms.title} description={meta.terms.description}
  path="/pt/termos/" altPath="/en/terms/"
  nav={nav} footer={footer}
  privacyPath="/pt/privacidade/" termsPath="/pt/termos/"
  linkedin={leadership.linkedin} github={leadership.github}
>
  <section class="section">
    <div class="container">
      <h1>{terms.title}</h1>
      {terms.sections.map((s) => (
        <div>
          <h2>{s.heading}</h2>
          <p>{s.text}</p>
        </div>
      ))}
    </div>
  </section>
</BaseLayout>
